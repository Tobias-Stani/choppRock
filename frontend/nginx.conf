server {
    listen 80;
    root /usr/share/nginx/html;
    client_max_body_size 50m;  # el Excel pesa varios MB (default de nginx: 1m)

    location = /admin { try_files /admin.html =404; }

    location /api/ {
        proxy_pass http://backend:8000;
        proxy_read_timeout 120s;
    }
}
